<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Farmer Dashboard</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>
<body>
<header>
  <div class="container header-container">
    <div class="logo">FarmConnect</div>
    <nav style="display:flex; align-items:center; gap:15px;">
      <a href="/">Home</a>
      <a href="/messages/inbox">Inbox</a>
      <a href="/logout">Logout</a>

      <!-- Header 3-dot menu -->
      <div style="position:relative;">
        <button id="header-menu-btn" style="background:none; border:none; font-size:20px; cursor:pointer;">&#8942;</button>
        <div id="header-menu-dropdown" style="display:none; position:absolute; right:0; background:white; border:1px solid #ccc; border-radius:6px; min-width:180px; box-shadow:0 2px 8px rgba(0,0,0,0.1); z-index:10;">
          <a href="/farmer/irrigation-page" style="display:block; padding:8px 12px; color:#333;">💧 Irrigation Status</a>
        </div>
      </div>
    </nav>
  </div>
</header>

<main class="container dashboard farmer-dashboard">
  <h1 class="center-text">Welcome, <%= session.user.name %></h1>

  <!-- Top row: Add Product + Buyer Requests -->
  <section class="cards-container">
    <!-- Add Product Form -->
    <div class="card">
      <h3><i class="fas fa-plus-circle"></i> Add Product</h3>
      <form action="/farmer/add-produce" method="POST">
        <input type="text" name="product_name" placeholder="Product Name" required>
        <input type="text" name="quantity" placeholder="Quantity (e.g., 100 kg)" required>
        <input type="text" name="price" placeholder="Price (e.g., ₹30 per kg)" required>
        <input type="date" name="harvest_date" required>
        <button class="btn" type="submit"><i class="fas fa-plus"></i> Add</button>
      </form>
    </div>

    <!-- Buyer Requests -->
    <div class="card">
      <h3><i class="fas fa-list-alt"></i> Buyer Requests</h3>
      <div class="table-wrapper">
        <table>
          <thead>
            <tr>
              <th>Buyer</th>
              <th>Crop</th>
              <th>Quantity</th>
              <th>Price</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% (buyerRequests || []).forEach(req => { %>
              <tr>
                <td><%= req.buyer_name %></td>
                <td><%= req.crop_name %></td>
                <td><%= req.quantity %></td>
                <td><%= req.price %></td>
                <td>
                  <form action="/messages/send" method="POST" style="display:flex; gap:5px; justify-content:center; align-items:center;">
                    <input type="hidden" name="receiver_id" value="<%= req.buyer_id %>">
                    <input type="text" name="message_text" placeholder="Type message..." class="msg-input" required>
                    <button class="btn" type="submit">Send</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- Your Products (Full Width) -->
  <section class="card product-list">
    <h3><i class="fas fa-box-open"></i> Product List</h3>
    <div class="table-wrapper">
      <table>
        <thead>
          <tr>
            <th>Product</th>
            <th>Quantity</th>
            <th>Price</th>
            <th>Harvest Date</th>
          </tr>
        </thead>
        <tbody>
          <% (productList || []).forEach(prod => { %>
            <tr>
              <td><%= prod.product_name %></td>
              <td><%= prod.quantity %></td>
              <td><%= prod.price %></td>
              <td><%= prod.harvest_date %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </section>
</main>

<script>
const headerMenuBtn = document.getElementById('header-menu-btn');
const headerMenuDropdown = document.getElementById('header-menu-dropdown');

headerMenuBtn.addEventListener('click', () => {
  headerMenuDropdown.style.display =
    headerMenuDropdown.style.display === 'none' ? 'block' : 'none';
});

// Close dropdown if clicked outside
document.addEventListener('click', function(event) {
  if (!headerMenuBtn.contains(event.target) && !headerMenuDropdown.contains(event.target)) {
    headerMenuDropdown.style.display = 'none';
  }
});
</script>

</body>
</html>
